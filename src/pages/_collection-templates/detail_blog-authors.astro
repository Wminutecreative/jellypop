---
import Layout from "../../layouts/Layout.astro";
---
{/*
  Webflow CMS Collection Page template (bound to a "Blog Authors"
  collection). The exported HTML for this template was empty — Webflow
  didn't bake any static markup into it, likely because the bound
  fields/components live entirely in dynamic embeds. You'll need to
  rebuild this one from the live Webflow project (Designer) or from the
  Author collection's field structure once you wire up real content.
*/}
<Layout title="Jellypop Studio" canonicalPath="/detail_blog-authors">
  <div class="page-wrapper">
    <!-- TODO: rebuild author template markup — export was empty -->
  </div>
</Layout>
